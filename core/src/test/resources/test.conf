# singleton schema
singleton {
  single.property = single.value

  list.property = [1, 2]

  single.ref = 1

  # references schema
  single.ref = 1

  # references schema
  list.ref = [1, 2]
}

# instances schema
instances {

  1 {
    single.property = single.value

    list.property = [1, 2]

    # references schema
    single.ref = 1

    # references schema
    list.ref = [1, 2]
  }

  2 {
    single.property = single.value

    # references schema
    list.property = [1, 2]

    # references schema
    single.ref = 1

    # references schema
    list.ref = [1, 2]
  }
}

# references schema
references {
  # empty instance
  1 { }

  # empty instance
  2 { }
}

# basic property
single.property = single.value
